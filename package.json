{
  "name": "tenant-banner-mock",
  "version": "0.1.0",
  "description": "Generic workflow-plumbing mock app for the FusionAuth Code Generation Agent. Mirrors the shape of the Tenant Login Maintenance Banner Linear issues without the FusionAuth Java stack.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
